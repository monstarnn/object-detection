storage:
 value: default
 wizard:
   name: "Storage Configuration"
   kind: cluster_storage

dataset:
  value: object-detection-pets
  workspace: kuberlab-demo
  version: "1.0.0"
  wizard:
    name: "Dataset configuration"
    kind: dataset

serving_port: 9000

